//this function show if user with special jwtToken have access to do special act on special data or object
const checkRoleAccess = async (jwtToken, object, act) => {
    // TODO return true or false
};