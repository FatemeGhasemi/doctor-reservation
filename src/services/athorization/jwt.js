const jwtGenerator = ({payload}) => {
    //TODO return jwtCode that sign with payload and secret

};

const verifyJwt = (jwtToken) => {
//    TODO return data that verify from jwtToken and secret
};


